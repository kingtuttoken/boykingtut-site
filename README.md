# BoyKingTut.com — Clean Phase 1

Fresh Astro static homepage for the new `boykingtut-site` repository.

This phase intentionally contains no Cloudflare adapter, Google OAuth, D1, RSS, old blog files, or lockfiles.
Deploy the homepage cleanly first. Add authentication/database as Phase 2 after this build is verified.

Build command: npm run build
Build output directory: dist
